const section_1 = document.getElementById("section_1"); // id (one)
const paragraphs = document.getElementsByClassName("red") // class name (one or many)
const articles = document.getElementsByTagName("article"); // tag name (one or many)

console.log(section_1);
console.log(paragraphs);
console.log(articles);