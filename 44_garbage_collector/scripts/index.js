let array = [1,2,3];

array = undefined;

// But the question is, where does [1,2,3] go ?
// [1,2,3] has been collected by garbage collector

// • Garbage collector: A feature in JS that automatically removes variables or objects from
// memory when they are no longer needed, to free up space.
